// tdashWrapper.ts
import { debounce as debounceDefault } from "./debounce";
import { throttle as throttleDefault } from "./throttle";

export const debounce = debounceDefault;
export const throttle = throttleDefault;
